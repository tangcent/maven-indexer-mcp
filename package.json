{
  "name": "maven-index",
  "version": "1.1.0",
  "private": true,
  "description": "Unified engine + CLI + MCP server over the local JVM artifact cache (renamed from maven-indexer-mcp)",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@maven-indexer/engine && npm run build --workspace=maven-indexer-mcp && npm run build --workspace=maven-indexer-cli",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.0.2",
    "@types/semver": "^7.7.1",
    "@types/xml2js": "^0.4.14",
    "@types/yauzl": "^2.10.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
